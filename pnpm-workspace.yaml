allowBuilds:
  # Vitest/Vite use esbuild's reviewed platform binary at runtime.
  esbuild: true
  # Windows installer wrapping is not part of local development or our release flow.
  electron-winstaller: false
